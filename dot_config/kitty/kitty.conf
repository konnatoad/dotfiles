# No transparency (fully opaque)
background_opacity 0.85

# Optional: font tweaks
font_family      JetBrainsMono Nerd Font
bold_font        auto
italic_font      auto
font_size        12.0

# Optional: padding
padding_width    5
padding_height   5

# Optional: color scheme fallback (if you don't use theme loader)
foreground       #c5c8c6
background       #1d1f21
